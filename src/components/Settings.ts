
export const Settings = Object.freeze({ 
    // --- ADD --- //
  
      grid: {
        dimension: 10,
        nodeSize: 60,
        nodeOffset: 10,
       
        color: 'rgba(245, 245, 245, 1)'
      }
    // --- ADD --- //
    })